// tailwind.config.js
tailwind.config = {
  darkMode: "class",
  theme: {
    extend: {
      // Extensiones de tema personalizadas
    },
  },
  variants: {
    extend: {
      // Variantes personalizadas
    },
  },
  plugins: [
    // Plugins adicionales
  ],
};
